# ربات تلگرام استعلام قطعی برق گلستان (Power Bot) ⚡️💡
> Telegram Bot for Golestan Province Electricity Outage & Blackout Schedules (GOPED)

این پروژه یک ربات کامل و پیشرفته تلگرام است که زمان‌بندی خاموشی‌های برق استان گلستان را مستقیماً از سامانه جدید شرکت توزیع نیروی برق گلستان (`modiriattolid.goped.ir`) دریافت کرده و به کاربران نمایش می‌دهد.

---

## ✨ ویژگی‌ها (Features)

- ⚡️ **استعلام سریع:** ارسال شناسه قبض ۱۳ رقمی و دریافت لحظه‌ای وضعیت خاموشی.
- 📅 **فیلترهای زمانی:** مشاهده برنامه **امروز**، **فردا** و **کل هفته** با دکمه‌های شیشه‌ای تعاملی.
- 🔖 **سیستم نشان‌شده‌ها (Bookmarks):** نشان کردن شناسه‌های مختلف با نام دلخواه و دسترسی ۱-کلیکه مستقیم از روی کیبورد.
- ⏰ **هشدار خودکار روزانه:** ارسال پیام یادآوری در صورت وجود قطعی برق در روز جاری (ساعت ۸:۰۰ صبح).
- 👥 **پشتیبانی از گروه‌ها:** افزودن ربات به گروه، تنظیم قبض توسط مدیر و ارسال هشدار روزانه مستقیماً در همان گروه.
- 📢 **ارسال پیام همگانی (`/shout`):** ارسال پیام، عکس یا فوروارد به تمامی کاربران ربات همراه با گزارش ارسال.
- 👥 **مشاهده آمار و لیست کاربران (`/users`):** مشاهده تعداد کاربران، نشان‌ها و مشخصات کاربران استارت‌زده.
- 🔢 **پشتیبانی کامل از ارقام فارسی:** تبدیل خودکار اعداد فارسی (`۱۲۳۴...`) به انگلیسی.
- 📢 **اطلاعیه‌های رسمی:** دریافت آخرین پیام‌ها و اطلاعیه‌های شرکت توزیع برق.
- ☁️ **پشتیبانی از دو روش اجرا:**
  - اجرای مستقیم با **Node.js / Docker** (روی سیستم یا سرور مجازی / VPS).
  - اجرای ۱۰۰٪ رایگان روی **Cloudflare Workers** (بدون نیاز به سرور).

---

## 🛠 جزئیات فنی و مهندسی معکوس API

وب‌سایت جدید شرکت توزیع برق بر پایه **Blazor WebAssembly** پیاده‌سازی شده است. از طریق بررسی و دیکامپایل اسمبلی‌های کلاینت:
- **آدرس اصلی API:** `https://modiriattolid.goped.ir:8090/`
- **هدر احراز هویت:** `Auth-Token: 7f3c2a91-6d84-4b17-a5e9-2c0f8d6b31a4`
- **اندپوینت استعلام زمان‌بندی:** `GET /Api/GetSchedule_Web?BillId={شناسه_قبض}`
- **اندپوینت اطلاعیه‌ها:** `GET /Api/GetNotice`
- **کپچا (CAPTCHA):** کپچای موجود در وب‌سایت صرفاً یک اعتبارسنجی سمت کلاینت (MudBlazor) بوده و اندپوینت API سرور نیازی به حل کپچا ندارد.

---

## 🚀 راهنمای راه‌اندازی (Setup Guide)

### ۱. دریافت توکن ربات تلگرام
1. در تلگرام وارد ربات [@BotFather](https://t.me/BotFather) شوید.
2. دستور `/newbot` را ارسال کرده و نام و یوزرنیم ربات خود را انتخاب کنید.
3. توکن داده شده (مثلاً `123456789:ABCdefGHIjkl...`) را کپی کنید.

### ۲. تنظیم متغیرهای محیطی
فایل `.env` را در ریشه پروژه باز کرده و توکن خود را در آن قرار دهید:
```env
BOT_TOKEN=توکن_ربات_شما
```

---

## 💻 روش‌های اجرا

### استفاده در گروه تلگرام

پس از افزودن ربات به گروه، یکی از مدیران گروه می‌تواند قبض و اعلان را تنظیم کند:

```text
/setbill 1234567890123 نام ساختمان
/check
/today
/tomorrow
/groupinfo
/notif on
/notif off
```

قبض‌ها و تنظیمات هر گروه با شناسهٔ همان گروه ذخیره می‌شوند و با حساب شخصی اعضا تداخلی ندارند. Cron اصلی Railway هر روز ساعت ۸ صبح به وقت تهران اجرا می‌شود و در صورت وجود خاموشی، اعلان را به خود گروه می‌فرستد.

### روش اول: استقرار ابری روی Railway 🚂 (پیشنهادی)

پروژه به صورت کامل برای استقرار روی پلتفرم **Railway** بهینه‌سازی شده است:

1. **پوش کردن به گیت‌هاب:** مخزن را در اکانت گیت‌هاب خود Push کنید.
2. **ایجاد پروژه جدید در Railway:**
   - وارد [Railway.app](https://railway.app) شوید.
   - روی **New Project** و سپس **Deploy from GitHub repo** کلیک کنید و مخزن را انتخاب نمایید.
3. **تنظیم متغیرهای محیطی (Variables):**
   - در داشبورد سرویس، وارد تب **Variables** شوید و مقادیر زیر را وارد کنید:
     ```env
     BOT_TOKEN=توکن_ربات_شما
     ADMIN_IDS=7250238664
     NOTIFICATION_CRON=0 8 * * *
     DB_FILE_PATH=/app/data/database.json
     PORT=3000
     ```
4. **اتصال Volume برای ذخیره دائمی دیتا (Persistent Storage):**
   - در داشبورد سرویس، به تب **Volumes** بروید و روی **Add Volume** کلیک کنید.
   - مسیر مانت (Mount Path) را روی `/app/data` بگذارید. (با این کار حتی در صورت ری‌استارت یا دیپلوی مجدد، دیتابیس کاربران و نشان‌ها هرگز پاک نخواهد شد).
5. **بررسی سلامت ربات:**
   - اندپوینت Health Check روی پورت ۳۰۰۰ به صورت خودکار فعال بوده و مسیر `/health` و `/stats` وضعیت آنلاین بودن ربات و تعداد کاربران را نمایش می‌دهد.

---

### روش دوم: اجرای مستقیم با Node.js
```bash
# نصب وابستگی‌ها
npm install

# مشاهده آمار دیتابیس
npm run db:stats

# اجرای تست‌ها برای اطمینان از سلامت اتصال به API
npm test

# اجرای ربات
npm start

# حالت توسعه (با راه‌اندازی مجدد خودکار)
npm run dev
```

### روش سوم: اجرا با Docker و Docker Compose
```bash
docker compose up -d --build
```

### روش چهارم: اجرا روی Cloudflare Workers (سرورلس)
ربات به صورت سرورلس روی Cloudflare Workers با حافظه KV نیز قابل استقرار است:

```bash
# استقرار یا بروزرسانی ربات روی کلادفلر
npm run cf:deploy

# تنظیم خودکار وب‌هوک و منوی دستورات تلگرام
npm run cf:setup

# مشاهده وضعیت لحظه‌ای وب‌هوک، مشخصات ربات و آمار کاربران
npm run cf:status

# مشاهده لاگ‌های زنده ورکر
npm run cf:logs
```

---

## 📁 ساختار پروژه (Project Structure)

```
Power Bot/
├── src/
│   ├── api/
│   │   └── gopedApi.js         # ارتباط با سرور و APIهای modiriattolid.goped.ir
│   ├── bot/
│   │   ├── formatters.js       # فرمت پیام‌های فارسی و جداول خاموشی
│   │   ├── handlers.js         # مدیریت پیام‌ها، دستورات و دکمه‌ها
│   │   └── keyboards.js        # کیبوردهای تلگرام
│   ├── db/
│   │   └── database.js         # دیتابیس JSON ذخیره کاربران و شناسه‌ها
│   ├── services/
│   │   └── notifier.js         # سرویس هشدار روزانه و کران‌جاب
│   ├── utils/
│   │   └── persianDate.js      # توابع تاریخ شمسی و ارقام فارسی
│   ├── config.js               # تنظیمات و متغیرهای محیطی
│   └── index.js                # نقطه شروع ربات (Node.js)
├── cf-worker/
│   ├── worker.js               # نسخه سرورلس کلادفلر ورکر
│   └── wrangler.toml
├── test/
│   └── test-api.js             # تست‌های خودکار سلامت API
├── .env.example
├── package.json
└── Dockerfile
```
